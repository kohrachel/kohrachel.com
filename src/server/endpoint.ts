// TODO: implement a wrapper function here for input schema validation for endpoints (use zod).
// they should be trpc style, so an endpoint can do something like export const listPosts = endpoint.input().execute() or similar
// consider the pros and cons of building this manually vs installing and using trpc for our use case before building
// to coding agents: do NOT build this unless directly instructed.
